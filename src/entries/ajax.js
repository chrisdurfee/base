/**
 * Ajax subpath entry.
 *
 * Importing from '@base-framework/base/ajax' pulls in only the Ajax module
 * (and its small dependencies) instead of the whole framework.
 */
export { Ajax, XhrRequest } from '../modules/ajax/ajax.js';
