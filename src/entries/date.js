/**
 * DateTime subpath entry.
 *
 * Importing from '@base-framework/base/date' pulls in only the DateTime
 * helpers instead of the whole framework.
 */
export { DateTime } from '../modules/date/date-time.js';
