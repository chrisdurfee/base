# Add any directories, files, or patterns you don't want to be tracked by version control
.vs
error_log
.editorconfig
.jshintrc
_notes/
.idea/
.vscode/
node_modules/
dist/
*.code-workspace
Dockerfile
docker-compose.yml

# Throwaway measurement harnesses and the variant source snapshots they
# load to run A/B comparisons. The baselines that are meant to be kept are
# bench-baseline.json and size-baseline.json, both tracked at the root.
.bench-phase0.js
perf-*.mjs
tmp/